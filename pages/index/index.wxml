<!--index.wxml-->
<view class="container">
  <view bindtap="shopDirect" wx:for="{{shops}}" wx:for-item="shop" wx:key="{{shop.id}}" data-shop="{{shop}}">
      <image class="shop-avatar" src="{{shop.image_path}}" />
      <text>{{shop.name}}</text>
  </view>
</view>

function Observe(pageOrComponent) {
    /**
     * 要使 Ui 更新, 必须通过 this.setData 来更新数据
     * 所以 mobx 中的 Observable 对象的改变, 需要调用该方法
     */

     function _update() {

     }

     function _setData() {

     }
     
}